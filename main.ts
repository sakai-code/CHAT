CHAT.onserver(function () {
    CHAT.messagetoserial()
    basic.showString(CHAT.receivedmessage())
})
input.onButtonPressed(Button.A, function () {
	
})
CHAT.server(1)
basic.forever(function () {
	
})
